type Request @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  description: String
  recipient: String
}

type Recipe @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
} 